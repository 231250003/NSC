int z;
int g(int x,int y){
    z=z+1;
   return 2*2+1;
}
int fib(int n) {
   if (n <= 1) {
    return n;
   }
   int x = fib(n - 1);
   int y = fib(n - 2);
   int z = x + y;
   return z;
}

int main() {
    int z;
    z=z+2;
    int p=(fib(g(1,1)))+z;
    return fib(p+g(2,2));
}