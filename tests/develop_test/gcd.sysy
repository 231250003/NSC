int gcd(int a, int b) {
    if (b == 0) {
        return a;  // 递归终止条件
    }
    return gcd(b, a % b);  // 递归调用
}
int main() {
    int num1=25, num2=40;
    int result = gcd(num1, num2);
    return result;
}