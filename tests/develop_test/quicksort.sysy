void swap(int arr[6], int low, int high) {
        int temp = arr[low];
         arr[low] = arr[high];
         arr[high] = temp;
}

// 打印数组
// 测试代码
int main() {
    int arr[6] = {10, 7, 8, 9, 1, 5};
    int n = 6;
    swap(arr, 1,n-1);
    return arr[1];
    //return 0;
}