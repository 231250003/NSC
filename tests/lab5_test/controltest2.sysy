int x = 56;
int y = 98;

int main() {
    int a = x;
    int b = y;
    while (b+a>20) {
        if (a > b+10) {
           if(a+b>10) a = a - b;
           else if(a>10) a=a-20;
           else b=b-5;
        } else {
            b = b - a;
        }
    }
    int result = a;
	return result;
}