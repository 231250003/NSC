int main() {



    // 循环控制测试
    int sum = 0;
    int n = 0;
     while (n < 5) {
       sum = sum + n;
       n = n + 1;
     }

     // 内存压力测试(无数组)
     int mem_test = 0;
     mem_test = c;       // 测试store指令
     int load_test = mem_test; // 测试load指令

    // 复合条件验证
    return sum;
}